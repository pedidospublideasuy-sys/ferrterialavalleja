const fs = require('fs'); let s = fs.readFileSync('src/components/admin/ProductForm.tsx', 'utf8'); s = s.replace(/import Image from 'next\/image';/, 'import Image from \'next/image\';\nimport ProductVariants from \'./ProductVariants\';'); s = s.replace(/<\/form>/, '</form>\n      {productId && <ProductVariants productId={productId} />}'); fs.writeFileSync('src/components/admin/ProductForm.tsx', s);
