const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({ datasources: { db: { url: 'postgresql://neondb_owner:npg_Bd38FNUIMSah@ep-winter-grass-b4d7zlkm-pooler.c-6.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require' } } }); async function main() { const p = await prisma.product.findMany({ orderBy: { updatedAt: 'desc' }, take: 10 }); p.forEach(x => console.log(x.name, x.images, x.stock)); } main().catch(console.error).finally(() => prisma['$disconnect']());
